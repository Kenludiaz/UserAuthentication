let mongoose = require('mongoose');

let Schema = mongoose.Schema;

let UserSchema = {
    y=username: _username,
    Password: _password,
    Age: _age,
    FullName: _fullname,
    Email: _email
}

